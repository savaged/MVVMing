<Window x:Class="CommunityMvvmToolkitDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommunityMvvmToolkitDemo"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        md:DialogServiceViews.IsRegistered="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Your Name:" Margin="0,0,10,0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="200" HorizontalAlignment="Left" Padding="5"/>

        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Content="Say Hello" Command="{Binding SayHelloCommand}" Margin="0,10,0,0" Padding="10"/>

        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Load Data" Command="{Binding LoadDataCommand}" Margin="0,10,0,0" Padding="10"
                IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBooleanConverter}}"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Data}" Margin="0,10,0,0" FontSize="16" FontWeight="Bold"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding GreetingResponse}" Margin="0,10,0,0" FontSize="16" FontWeight="Bold"/>

        <ProgressBar Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="20" Margin="0,10,0,0"/>

    </Grid>
</Window>
